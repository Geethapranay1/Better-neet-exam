import type { Options } from './types';
declare const CryptoPuzzle: {
    generate: (options: Options) => Promise<Uint8Array>;
    solve: (puzzle: Uint8Array) => Promise<string>;
};
export default CryptoPuzzle;
export type { Options };
